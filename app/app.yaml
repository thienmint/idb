runtime: python27
api_version: 1
threadsafe: true

# [START handlers]
handlers:
- url: /static
  static_dir: static
- url: /.*
  script: app.app
# [END handlers]

# [START libraries]
libraries:
 - name: jinja2
   version: latest
 - name: MySQLdb
   version: "1.2.5"
# [END libraries]
